profiles:
  default:
    fqbn: arduino:zephyr:unoq
    platforms:
      - platform: arduino:zephyr
    libraries:
      # Arduino_RouterBridge (le Bridge) est fourni par le cœur arduino:zephyr
      # récent -> ne pas le lister explicitement. On ne déclare que Servo.
      - Servo

default_profile: default
