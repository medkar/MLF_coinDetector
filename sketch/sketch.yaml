profiles:
  default:
    fqbn: arduino:zephyr:unoq
    platforms:
      - platform: arduino:zephyr
    libraries:
      # Servo 1.3.0 = première version qui supporte les cartes Zephyr (UNO Q).
      # La version explicite est requise (sinon "invalid library reference: Servo ()").
      # Arduino_RouterBridge (le Bridge) est fourni par le cœur arduino:zephyr récent.
      - Servo (1.3.0)

default_profile: default
